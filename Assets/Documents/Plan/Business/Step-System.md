# 步骤系统

## 概述

实验流程的核心执行引擎。**不完全遵循 MCV**——步骤链较长，为便于策划编辑，大量内容在 GameObject 层面配置为预制体，运行时由步骤调度器按序实例化执行。Controller 在此退化为轻量调度器，步骤逻辑内聚在预制体自身。主要用于电路连接、仿真实验等环节。

## 待完善

- 步骤预制体设计规范（接口、生命周期钩子）
- 步骤调度器（加载、执行、完成判定、切换、回退）
- 与 MCV Controller 的协作边界
